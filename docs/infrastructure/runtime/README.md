# Runtime Infrastructure Documents

This directory owns reusable lifecycle and control-plane infrastructure:
service supervision, operator commands, run/release control, endpoint
allocation, runtime management and reproducible host toolchain provisioning.
It must not become a generic service locator or scientific-state owner.

See `JAVA_TOOLCHAIN_PROVISIONING.md` for the Java/Temurin acquisition, verification and receipt authority boundary.
