# Round 19 — Generic Service Supervisor

- Generic immutable launch contract for model/Study/environment services.
- Exact process identity reconciliation before reuse or kill.
- READY evidence bound to contract + process + stdout/stderr capture references.
- Stable exit classes and bounded same-contract TEMPORARY restart policy.
- Durable atomic supervisor state.
- Deterministic Type=notify systemd unit renderer with no quality fallback logic.
